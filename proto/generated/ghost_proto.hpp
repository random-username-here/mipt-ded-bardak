// GENERATED FILE -- DO NOT EDIT
// Generated by libpan
// Contains:
//  - server ghost:wall(...)
//  - server ghost:sees(...)
//  - server ghost:where(...)
//  - server ghost:at(...)
//  - server ghost:hp(...)
//  - server ghost:tick(...)
//  - client ghost:sees(...)
//  - client ghost:where(...)
//  - client ghost:attack(...)
//  - client ghost:move(...)

#pragma once
#include "libpan_cxx_macros.hpp"

PAN_GH_DEFS_BEGIN

PAN_GH_MSG(PAN_GH_SERVER, ghost, wall, "ghost", "wall",
    int32_t          x;
    int32_t          y;
)
PAN_GH_DECODE(PAN_GH_SERVER, ghost, wall, "ghost", "wall",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
)
PAN_GH_ENCODE(PAN_GH_SERVER, ghost, wall, "ghost", "wall",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    PAN_GH_HEADER("ghost", "wall", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
)

PAN_GH_MSG(PAN_GH_SERVER, ghost, sees, "ghost", "sees",
    int32_t          x;
    int32_t          y;
    PAN_GH_ID        who;
    PAN_GH_ID        teamId;
)
PAN_GH_DECODE(PAN_GH_SERVER, ghost, sees, "ghost", "sees",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
    PAN_GH_READ(&self->who, sizeof(self->who));
    PAN_GH_READ(&self->teamId, sizeof(self->teamId));
)
PAN_GH_ENCODE(PAN_GH_SERVER, ghost, sees, "ghost", "sees",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    len += sizeof(self->who);
    len += sizeof(self->teamId);
    PAN_GH_HEADER("ghost", "sees", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
    PAN_GH_WRITE(&self->who, sizeof(self->who));
    PAN_GH_WRITE(&self->teamId, sizeof(self->teamId));
)

PAN_GH_MSG(PAN_GH_SERVER, ghost, where, "ghost", "where",
    int32_t          x;
    int32_t          y;
    PAN_GH_ID        who;
    PAN_GH_ID        teamId;
)
PAN_GH_DECODE(PAN_GH_SERVER, ghost, where, "ghost", "where",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
    PAN_GH_READ(&self->who, sizeof(self->who));
    PAN_GH_READ(&self->teamId, sizeof(self->teamId));
)
PAN_GH_ENCODE(PAN_GH_SERVER, ghost, where, "ghost", "where",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    len += sizeof(self->who);
    len += sizeof(self->teamId);
    PAN_GH_HEADER("ghost", "where", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
    PAN_GH_WRITE(&self->who, sizeof(self->who));
    PAN_GH_WRITE(&self->teamId, sizeof(self->teamId));
)

PAN_GH_MSG(PAN_GH_SERVER, ghost, at, "ghost", "at",
    int32_t          x;
    int32_t          y;
)
PAN_GH_DECODE(PAN_GH_SERVER, ghost, at, "ghost", "at",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
)
PAN_GH_ENCODE(PAN_GH_SERVER, ghost, at, "ghost", "at",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    PAN_GH_HEADER("ghost", "at", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
)

PAN_GH_MSG(PAN_GH_SERVER, ghost, hp, "ghost", "hp",
    int32_t          val;
)
PAN_GH_DECODE(PAN_GH_SERVER, ghost, hp, "ghost", "hp",
    PAN_GH_READ(&self->val, sizeof(self->val));
)
PAN_GH_ENCODE(PAN_GH_SERVER, ghost, hp, "ghost", "hp",
    uint16_t len = 0;
    len += sizeof(self->val);
    PAN_GH_HEADER("ghost", "hp", len);
    PAN_GH_WRITE(&self->val, sizeof(self->val));
)

PAN_GH_MSG(PAN_GH_SERVER, ghost, tick, "ghost", "tick",
)
PAN_GH_DECODE(PAN_GH_SERVER, ghost, tick, "ghost", "tick",
)
PAN_GH_ENCODE(PAN_GH_SERVER, ghost, tick, "ghost", "tick",
    uint16_t len = 0;
    PAN_GH_HEADER("ghost", "tick", len);
)

PAN_GH_MSG(PAN_GH_CLIENT, ghost, sees, "ghost", "sees",
)
PAN_GH_DECODE(PAN_GH_CLIENT, ghost, sees, "ghost", "sees",
)
PAN_GH_ENCODE(PAN_GH_CLIENT, ghost, sees, "ghost", "sees",
    uint16_t len = 0;
    PAN_GH_HEADER("ghost", "sees", len);
)

PAN_GH_MSG(PAN_GH_CLIENT, ghost, where, "ghost", "where",
    PAN_GH_ID        teamId;
)
PAN_GH_DECODE(PAN_GH_CLIENT, ghost, where, "ghost", "where",
    PAN_GH_READ(&self->teamId, sizeof(self->teamId));
)
PAN_GH_ENCODE(PAN_GH_CLIENT, ghost, where, "ghost", "where",
    uint16_t len = 0;
    len += sizeof(self->teamId);
    PAN_GH_HEADER("ghost", "where", len);
    PAN_GH_WRITE(&self->teamId, sizeof(self->teamId));
)

PAN_GH_MSG(PAN_GH_CLIENT, ghost, attack, "ghost", "attack",
    PAN_GH_ID        whom;
)
PAN_GH_DECODE(PAN_GH_CLIENT, ghost, attack, "ghost", "attack",
    PAN_GH_READ(&self->whom, sizeof(self->whom));
)
PAN_GH_ENCODE(PAN_GH_CLIENT, ghost, attack, "ghost", "attack",
    uint16_t len = 0;
    len += sizeof(self->whom);
    PAN_GH_HEADER("ghost", "attack", len);
    PAN_GH_WRITE(&self->whom, sizeof(self->whom));
)

PAN_GH_MSG(PAN_GH_CLIENT, ghost, move, "ghost", "move",
    int8_t           dx;
    int8_t           dy;
)
PAN_GH_DECODE(PAN_GH_CLIENT, ghost, move, "ghost", "move",
    PAN_GH_READ(&self->dx, sizeof(self->dx));
    PAN_GH_READ(&self->dy, sizeof(self->dy));
)
PAN_GH_ENCODE(PAN_GH_CLIENT, ghost, move, "ghost", "move",
    uint16_t len = 0;
    len += sizeof(self->dx);
    len += sizeof(self->dy);
    PAN_GH_HEADER("ghost", "move", len);
    PAN_GH_WRITE(&self->dx, sizeof(self->dx));
    PAN_GH_WRITE(&self->dy, sizeof(self->dy));
)

PAN_GH_DEFS_END
