// GENERATED FILE -- DO NOT EDIT
// Generated by libpan
// Contains:
//  - server gmap:r.vis(...)
//  - client gmap:getVis(...)
//  - server gmap:r.move(...)
//  - client gmap:move(...)
//  - server gmap:map(...)

#pragma once
#include "libpan_cxx_macros.hpp"

PAN_GH_DEFS_BEGIN

PAN_GH_MSG(PAN_GH_SERVER, gmap, r_vis, "gmap", "r.vis",
    PAN_GH_ID        unitId;
    PAN_GH_CHAR64    unitType;
    int8_t           dx;
    int8_t           dy;
)
PAN_GH_DECODE(PAN_GH_SERVER, gmap, r_vis, "gmap", "r.vis",
    PAN_GH_READ(&self->unitId, sizeof(self->unitId));
    PAN_GH_READ(&self->unitType, sizeof(self->unitType));
    PAN_GH_READ(&self->dx, sizeof(self->dx));
    PAN_GH_READ(&self->dy, sizeof(self->dy));
)
PAN_GH_ENCODE(PAN_GH_SERVER, gmap, r_vis, "gmap", "r.vis",
    uint16_t len = 0;
    len += sizeof(self->unitId);
    len += sizeof(self->unitType);
    len += sizeof(self->dx);
    len += sizeof(self->dy);
    PAN_GH_HEADER("gmap", "r.vis", len);
    PAN_GH_WRITE(&self->unitId, sizeof(self->unitId));
    PAN_GH_WRITE(&self->unitType, sizeof(self->unitType));
    PAN_GH_WRITE(&self->dx, sizeof(self->dx));
    PAN_GH_WRITE(&self->dy, sizeof(self->dy));
)

PAN_GH_MSG(PAN_GH_CLIENT, gmap, getVis, "gmap", "getVis",
)
PAN_GH_DECODE(PAN_GH_CLIENT, gmap, getVis, "gmap", "getVis",
)
PAN_GH_ENCODE(PAN_GH_CLIENT, gmap, getVis, "gmap", "getVis",
    uint16_t len = 0;
    PAN_GH_HEADER("gmap", "getVis", len);
)

PAN_GH_MSG(PAN_GH_SERVER, gmap, r_move, "gmap", "r.move",
    PAN_GH_ID        req;
    bool             ok;
)
PAN_GH_DECODE(PAN_GH_SERVER, gmap, r_move, "gmap", "r.move",
    PAN_GH_READ(&self->req, sizeof(self->req));
    PAN_GH_READ(&self->ok, sizeof(self->ok));
)
PAN_GH_ENCODE(PAN_GH_SERVER, gmap, r_move, "gmap", "r.move",
    uint16_t len = 0;
    len += sizeof(self->req);
    len += sizeof(self->ok);
    PAN_GH_HEADER("gmap", "r.move", len);
    PAN_GH_WRITE(&self->req, sizeof(self->req));
    PAN_GH_WRITE(&self->ok, sizeof(self->ok));
)

PAN_GH_MSG(PAN_GH_CLIENT, gmap, move, "gmap", "move",
    int8_t           dx;
    int8_t           dy;
)
PAN_GH_DECODE(PAN_GH_CLIENT, gmap, move, "gmap", "move",
    PAN_GH_READ(&self->dx, sizeof(self->dx));
    PAN_GH_READ(&self->dy, sizeof(self->dy));
)
PAN_GH_ENCODE(PAN_GH_CLIENT, gmap, move, "gmap", "move",
    uint16_t len = 0;
    len += sizeof(self->dx);
    len += sizeof(self->dy);
    PAN_GH_HEADER("gmap", "move", len);
    PAN_GH_WRITE(&self->dx, sizeof(self->dx));
    PAN_GH_WRITE(&self->dy, sizeof(self->dy));
)

PAN_GH_MSG(PAN_GH_SERVER, gmap, map, "gmap", "map",
    int16_t          width;
    int16_t          height;
    PAN_GH_SLICE     data;
)
PAN_GH_DECODE(PAN_GH_SERVER, gmap, map, "gmap", "map",
    PAN_GH_READ(&self->width, sizeof(self->width));
    PAN_GH_READ(&self->height, sizeof(self->height));
    uint16_t data_len = 0;
    PAN_GH_READ(&data_len, 2);
    PAN_GH_READ_SLICE(&self->data, data_len);
)
PAN_GH_ENCODE(PAN_GH_SERVER, gmap, map, "gmap", "map",
    uint16_t len = 0;
    len += sizeof(self->width);
    len += sizeof(self->height);
    len += 2 + PAN_GH_SLICE_LEN(&self->data);
    PAN_GH_HEADER("gmap", "map", len);
    PAN_GH_WRITE(&self->width, sizeof(self->width));
    PAN_GH_WRITE(&self->height, sizeof(self->height));
    uint16_t data_len = PAN_GH_SLICE_LEN(&self->data);
    PAN_GH_WRITE(&data_len, 2);
    PAN_GH_WRITE_SLICE(&self->data, data_len);
)

PAN_GH_DEFS_END
