// GENERATED FILE -- DO NOT EDIT
// Generated by libpan
// Contains:
//  - server role:reject(...)
//  - server role:chosen(...)
//  - server role:option(...)
//  - client role:choose(...)

#pragma once
#include "libpan_cxx_macros.hpp"

PAN_GH_DEFS_BEGIN

PAN_GH_MSG(PAN_GH_SERVER, role, reject, "role", "reject",
    PAN_GH_SLICE     reason;
)
PAN_GH_DECODE(PAN_GH_SERVER, role, reject, "role", "reject",
    uint16_t reason_len = 0;
    PAN_GH_READ(&reason_len, 2);
    PAN_GH_READ_SLICE(&self->reason, reason_len);
)
PAN_GH_ENCODE(PAN_GH_SERVER, role, reject, "role", "reject",
    uint16_t len = 0;
    len += 2 + PAN_GH_SLICE_LEN(&self->reason);
    PAN_GH_HEADER("role", "reject", len);
    uint16_t reason_len = PAN_GH_SLICE_LEN(&self->reason);
    PAN_GH_WRITE(&reason_len, 2);
    PAN_GH_WRITE_SLICE(&self->reason, reason_len);
)

PAN_GH_MSG(PAN_GH_SERVER, role, chosen, "role", "chosen",
    PAN_GH_SLICE     id;
)
PAN_GH_DECODE(PAN_GH_SERVER, role, chosen, "role", "chosen",
    uint16_t id_len = 0;
    PAN_GH_READ(&id_len, 2);
    PAN_GH_READ_SLICE(&self->id, id_len);
)
PAN_GH_ENCODE(PAN_GH_SERVER, role, chosen, "role", "chosen",
    uint16_t len = 0;
    len += 2 + PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_HEADER("role", "chosen", len);
    uint16_t id_len = PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_WRITE(&id_len, 2);
    PAN_GH_WRITE_SLICE(&self->id, id_len);
)

PAN_GH_MSG(PAN_GH_SERVER, role, option, "role", "option",
    PAN_GH_SLICE     id;
    PAN_GH_SLICE     name;
    PAN_GH_SLICE     prefix;
)
PAN_GH_DECODE(PAN_GH_SERVER, role, option, "role", "option",
    uint16_t id_len = 0;
    PAN_GH_READ(&id_len, 2);
    PAN_GH_READ_SLICE(&self->id, id_len);
    uint16_t name_len = 0;
    PAN_GH_READ(&name_len, 2);
    PAN_GH_READ_SLICE(&self->name, name_len);
    uint16_t prefix_len = 0;
    PAN_GH_READ(&prefix_len, 2);
    PAN_GH_READ_SLICE(&self->prefix, prefix_len);
)
PAN_GH_ENCODE(PAN_GH_SERVER, role, option, "role", "option",
    uint16_t len = 0;
    len += 2 + PAN_GH_SLICE_LEN(&self->id);
    len += 2 + PAN_GH_SLICE_LEN(&self->name);
    len += 2 + PAN_GH_SLICE_LEN(&self->prefix);
    PAN_GH_HEADER("role", "option", len);
    uint16_t id_len = PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_WRITE(&id_len, 2);
    PAN_GH_WRITE_SLICE(&self->id, id_len);
    uint16_t name_len = PAN_GH_SLICE_LEN(&self->name);
    PAN_GH_WRITE(&name_len, 2);
    PAN_GH_WRITE_SLICE(&self->name, name_len);
    uint16_t prefix_len = PAN_GH_SLICE_LEN(&self->prefix);
    PAN_GH_WRITE(&prefix_len, 2);
    PAN_GH_WRITE_SLICE(&self->prefix, prefix_len);
)

PAN_GH_MSG(PAN_GH_CLIENT, role, choose, "role", "choose",
    PAN_GH_SLICE     id;
)
PAN_GH_DECODE(PAN_GH_CLIENT, role, choose, "role", "choose",
    uint16_t id_len = 0;
    PAN_GH_READ(&id_len, 2);
    PAN_GH_READ_SLICE(&self->id, id_len);
)
PAN_GH_ENCODE(PAN_GH_CLIENT, role, choose, "role", "choose",
    uint16_t len = 0;
    len += 2 + PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_HEADER("role", "choose", len);
    uint16_t id_len = PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_WRITE(&id_len, 2);
    PAN_GH_WRITE_SLICE(&self->id, id_len);
)

PAN_GH_DEFS_END
