// GENERATED FILE -- DO NOT EDIT
// Generated by libpan
// Contains:
//  - server rogue:ability(...)
//  - server rogue:item(...)
//  - server rogue:wall(...)
//  - server rogue:enemy(...)
//  - server rogue:root(...)
//  - server rogue:at(...)
//  - server rogue:hp(...)
//  - server rogue:tick(...)
//  - client rogue:use(...)
//  - client rogue:move(...)

#pragma once
#include "libpan_cxx_macros.hpp"

PAN_GH_DEFS_BEGIN

PAN_GH_MSG(PAN_GH_SERVER, rogue, ability, "rogue", "ability",
    PAN_GH_SLICE     id;
)
PAN_GH_DECODE(PAN_GH_SERVER, rogue, ability, "rogue", "ability",
    uint16_t id_len = 0;
    PAN_GH_READ(&id_len, 2);
    PAN_GH_READ_SLICE(&self->id, id_len);
)
PAN_GH_ENCODE(PAN_GH_SERVER, rogue, ability, "rogue", "ability",
    uint16_t len = 0;
    len += 2 + PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_HEADER("rogue", "ability", len);
    uint16_t id_len = PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_WRITE(&id_len, 2);
    PAN_GH_WRITE_SLICE(&self->id, id_len);
)

PAN_GH_MSG(PAN_GH_SERVER, rogue, item, "rogue", "item",
    PAN_GH_SLICE     id;
)
PAN_GH_DECODE(PAN_GH_SERVER, rogue, item, "rogue", "item",
    uint16_t id_len = 0;
    PAN_GH_READ(&id_len, 2);
    PAN_GH_READ_SLICE(&self->id, id_len);
)
PAN_GH_ENCODE(PAN_GH_SERVER, rogue, item, "rogue", "item",
    uint16_t len = 0;
    len += 2 + PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_HEADER("rogue", "item", len);
    uint16_t id_len = PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_WRITE(&id_len, 2);
    PAN_GH_WRITE_SLICE(&self->id, id_len);
)

PAN_GH_MSG(PAN_GH_SERVER, rogue, wall, "rogue", "wall",
    int32_t          x;
    int32_t          y;
)
PAN_GH_DECODE(PAN_GH_SERVER, rogue, wall, "rogue", "wall",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
)
PAN_GH_ENCODE(PAN_GH_SERVER, rogue, wall, "rogue", "wall",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    PAN_GH_HEADER("rogue", "wall", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
)

PAN_GH_MSG(PAN_GH_SERVER, rogue, enemy, "rogue", "enemy",
    int32_t          x;
    int32_t          y;
    PAN_GH_ID        who;
    PAN_GH_SLICE     kind;
)
PAN_GH_DECODE(PAN_GH_SERVER, rogue, enemy, "rogue", "enemy",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
    PAN_GH_READ(&self->who, sizeof(self->who));
    uint16_t kind_len = 0;
    PAN_GH_READ(&kind_len, 2);
    PAN_GH_READ_SLICE(&self->kind, kind_len);
)
PAN_GH_ENCODE(PAN_GH_SERVER, rogue, enemy, "rogue", "enemy",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    len += sizeof(self->who);
    len += 2 + PAN_GH_SLICE_LEN(&self->kind);
    PAN_GH_HEADER("rogue", "enemy", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
    PAN_GH_WRITE(&self->who, sizeof(self->who));
    uint16_t kind_len = PAN_GH_SLICE_LEN(&self->kind);
    PAN_GH_WRITE(&kind_len, 2);
    PAN_GH_WRITE_SLICE(&self->kind, kind_len);
)

PAN_GH_MSG(PAN_GH_SERVER, rogue, root, "rogue", "root",
    int32_t          x;
    int32_t          y;
    PAN_GH_ID        who;
)
PAN_GH_DECODE(PAN_GH_SERVER, rogue, root, "rogue", "root",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
    PAN_GH_READ(&self->who, sizeof(self->who));
)
PAN_GH_ENCODE(PAN_GH_SERVER, rogue, root, "rogue", "root",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    len += sizeof(self->who);
    PAN_GH_HEADER("rogue", "root", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
    PAN_GH_WRITE(&self->who, sizeof(self->who));
)

PAN_GH_MSG(PAN_GH_SERVER, rogue, at, "rogue", "at",
    int32_t          x;
    int32_t          y;
)
PAN_GH_DECODE(PAN_GH_SERVER, rogue, at, "rogue", "at",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
)
PAN_GH_ENCODE(PAN_GH_SERVER, rogue, at, "rogue", "at",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    PAN_GH_HEADER("rogue", "at", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
)

PAN_GH_MSG(PAN_GH_SERVER, rogue, hp, "rogue", "hp",
    int32_t          val;
)
PAN_GH_DECODE(PAN_GH_SERVER, rogue, hp, "rogue", "hp",
    PAN_GH_READ(&self->val, sizeof(self->val));
)
PAN_GH_ENCODE(PAN_GH_SERVER, rogue, hp, "rogue", "hp",
    uint16_t len = 0;
    len += sizeof(self->val);
    PAN_GH_HEADER("rogue", "hp", len);
    PAN_GH_WRITE(&self->val, sizeof(self->val));
)

PAN_GH_MSG(PAN_GH_SERVER, rogue, tick, "rogue", "tick",
)
PAN_GH_DECODE(PAN_GH_SERVER, rogue, tick, "rogue", "tick",
)
PAN_GH_ENCODE(PAN_GH_SERVER, rogue, tick, "rogue", "tick",
    uint16_t len = 0;
    PAN_GH_HEADER("rogue", "tick", len);
)

PAN_GH_MSG(PAN_GH_CLIENT, rogue, use, "rogue", "use",
    PAN_GH_SLICE     ability;
    PAN_GH_ID        target;
)
PAN_GH_DECODE(PAN_GH_CLIENT, rogue, use, "rogue", "use",
    uint16_t ability_len = 0;
    PAN_GH_READ(&ability_len, 2);
    PAN_GH_READ_SLICE(&self->ability, ability_len);
    PAN_GH_READ(&self->target, sizeof(self->target));
)
PAN_GH_ENCODE(PAN_GH_CLIENT, rogue, use, "rogue", "use",
    uint16_t len = 0;
    len += 2 + PAN_GH_SLICE_LEN(&self->ability);
    len += sizeof(self->target);
    PAN_GH_HEADER("rogue", "use", len);
    uint16_t ability_len = PAN_GH_SLICE_LEN(&self->ability);
    PAN_GH_WRITE(&ability_len, 2);
    PAN_GH_WRITE_SLICE(&self->ability, ability_len);
    PAN_GH_WRITE(&self->target, sizeof(self->target));
)

PAN_GH_MSG(PAN_GH_CLIENT, rogue, move, "rogue", "move",
    int8_t           dx;
    int8_t           dy;
)
PAN_GH_DECODE(PAN_GH_CLIENT, rogue, move, "rogue", "move",
    PAN_GH_READ(&self->dx, sizeof(self->dx));
    PAN_GH_READ(&self->dy, sizeof(self->dy));
)
PAN_GH_ENCODE(PAN_GH_CLIENT, rogue, move, "rogue", "move",
    uint16_t len = 0;
    len += sizeof(self->dx);
    len += sizeof(self->dy);
    PAN_GH_HEADER("rogue", "move", len);
    PAN_GH_WRITE(&self->dx, sizeof(self->dx));
    PAN_GH_WRITE(&self->dy, sizeof(self->dy));
)

PAN_GH_DEFS_END
