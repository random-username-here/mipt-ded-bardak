// GENERATED FILE -- DO NOT EDIT
// Generated by libpan
// Contains:
//  - server paladin:ability(...)
//  - server paladin:item(...)
//  - server paladin:wall(...)
//  - server paladin:enemy(...)
//  - server paladin:root(...)
//  - server paladin:at(...)
//  - server paladin:hp(...)
//  - server paladin:tick(...)
//  - client paladin:use(...)
//  - client paladin:move(...)

#pragma once
#include "libpan_cxx_macros.hpp"

PAN_GH_DEFS_BEGIN

PAN_GH_MSG(PAN_GH_SERVER, paladin, ability, "paladin", "ability",
    PAN_GH_SLICE     id;
)
PAN_GH_DECODE(PAN_GH_SERVER, paladin, ability, "paladin", "ability",
    uint16_t id_len = 0;
    PAN_GH_READ(&id_len, 2);
    PAN_GH_READ_SLICE(&self->id, id_len);
)
PAN_GH_ENCODE(PAN_GH_SERVER, paladin, ability, "paladin", "ability",
    uint16_t len = 0;
    len += 2 + PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_HEADER("paladin", "ability", len);
    uint16_t id_len = PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_WRITE(&id_len, 2);
    PAN_GH_WRITE_SLICE(&self->id, id_len);
)

PAN_GH_MSG(PAN_GH_SERVER, paladin, item, "paladin", "item",
    PAN_GH_SLICE     id;
)
PAN_GH_DECODE(PAN_GH_SERVER, paladin, item, "paladin", "item",
    uint16_t id_len = 0;
    PAN_GH_READ(&id_len, 2);
    PAN_GH_READ_SLICE(&self->id, id_len);
)
PAN_GH_ENCODE(PAN_GH_SERVER, paladin, item, "paladin", "item",
    uint16_t len = 0;
    len += 2 + PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_HEADER("paladin", "item", len);
    uint16_t id_len = PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_WRITE(&id_len, 2);
    PAN_GH_WRITE_SLICE(&self->id, id_len);
)

PAN_GH_MSG(PAN_GH_SERVER, paladin, wall, "paladin", "wall",
    int32_t          x;
    int32_t          y;
)
PAN_GH_DECODE(PAN_GH_SERVER, paladin, wall, "paladin", "wall",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
)
PAN_GH_ENCODE(PAN_GH_SERVER, paladin, wall, "paladin", "wall",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    PAN_GH_HEADER("paladin", "wall", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
)

PAN_GH_MSG(PAN_GH_SERVER, paladin, enemy, "paladin", "enemy",
    int32_t          x;
    int32_t          y;
    PAN_GH_ID        who;
    PAN_GH_CHAR64    kind;
)
PAN_GH_DECODE(PAN_GH_SERVER, paladin, enemy, "paladin", "enemy",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
    PAN_GH_READ(&self->who, sizeof(self->who));
    PAN_GH_READ(&self->kind, sizeof(self->kind));
)
PAN_GH_ENCODE(PAN_GH_SERVER, paladin, enemy, "paladin", "enemy",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    len += sizeof(self->who);
    len += sizeof(self->kind);
    PAN_GH_HEADER("paladin", "enemy", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
    PAN_GH_WRITE(&self->who, sizeof(self->who));
    PAN_GH_WRITE(&self->kind, sizeof(self->kind));
)

PAN_GH_MSG(PAN_GH_SERVER, paladin, root, "paladin", "root",
    int32_t          x;
    int32_t          y;
    PAN_GH_ID        who;
)
PAN_GH_DECODE(PAN_GH_SERVER, paladin, root, "paladin", "root",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
    PAN_GH_READ(&self->who, sizeof(self->who));
)
PAN_GH_ENCODE(PAN_GH_SERVER, paladin, root, "paladin", "root",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    len += sizeof(self->who);
    PAN_GH_HEADER("paladin", "root", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
    PAN_GH_WRITE(&self->who, sizeof(self->who));
)

PAN_GH_MSG(PAN_GH_SERVER, paladin, at, "paladin", "at",
    int32_t          x;
    int32_t          y;
)
PAN_GH_DECODE(PAN_GH_SERVER, paladin, at, "paladin", "at",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
)
PAN_GH_ENCODE(PAN_GH_SERVER, paladin, at, "paladin", "at",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    PAN_GH_HEADER("paladin", "at", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
)

PAN_GH_MSG(PAN_GH_SERVER, paladin, hp, "paladin", "hp",
    int32_t          val;
)
PAN_GH_DECODE(PAN_GH_SERVER, paladin, hp, "paladin", "hp",
    PAN_GH_READ(&self->val, sizeof(self->val));
)
PAN_GH_ENCODE(PAN_GH_SERVER, paladin, hp, "paladin", "hp",
    uint16_t len = 0;
    len += sizeof(self->val);
    PAN_GH_HEADER("paladin", "hp", len);
    PAN_GH_WRITE(&self->val, sizeof(self->val));
)

PAN_GH_MSG(PAN_GH_SERVER, paladin, tick, "paladin", "tick",
)
PAN_GH_DECODE(PAN_GH_SERVER, paladin, tick, "paladin", "tick",
)
PAN_GH_ENCODE(PAN_GH_SERVER, paladin, tick, "paladin", "tick",
    uint16_t len = 0;
    PAN_GH_HEADER("paladin", "tick", len);
)

PAN_GH_MSG(PAN_GH_CLIENT, paladin, use, "paladin", "use",
    PAN_GH_SLICE     ability;
    PAN_GH_ID        target;
)
PAN_GH_DECODE(PAN_GH_CLIENT, paladin, use, "paladin", "use",
    uint16_t ability_len = 0;
    PAN_GH_READ(&ability_len, 2);
    PAN_GH_READ_SLICE(&self->ability, ability_len);
    PAN_GH_READ(&self->target, sizeof(self->target));
)
PAN_GH_ENCODE(PAN_GH_CLIENT, paladin, use, "paladin", "use",
    uint16_t len = 0;
    len += 2 + PAN_GH_SLICE_LEN(&self->ability);
    len += sizeof(self->target);
    PAN_GH_HEADER("paladin", "use", len);
    uint16_t ability_len = PAN_GH_SLICE_LEN(&self->ability);
    PAN_GH_WRITE(&ability_len, 2);
    PAN_GH_WRITE_SLICE(&self->ability, ability_len);
    PAN_GH_WRITE(&self->target, sizeof(self->target));
)

PAN_GH_MSG(PAN_GH_CLIENT, paladin, move, "paladin", "move",
    int8_t           dx;
    int8_t           dy;
)
PAN_GH_DECODE(PAN_GH_CLIENT, paladin, move, "paladin", "move",
    PAN_GH_READ(&self->dx, sizeof(self->dx));
    PAN_GH_READ(&self->dy, sizeof(self->dy));
)
PAN_GH_ENCODE(PAN_GH_CLIENT, paladin, move, "paladin", "move",
    uint16_t len = 0;
    len += sizeof(self->dx);
    len += sizeof(self->dy);
    PAN_GH_HEADER("paladin", "move", len);
    PAN_GH_WRITE(&self->dx, sizeof(self->dx));
    PAN_GH_WRITE(&self->dy, sizeof(self->dy));
)

PAN_GH_DEFS_END
