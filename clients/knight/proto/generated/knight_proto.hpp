// GENERATED FILE -- DO NOT EDIT
// Generated by libpan
// Contains:
//  - server knight:ability(...)
//  - server knight:item(...)
//  - server knight:wall(...)
//  - server knight:enemy(...)
//  - server knight:root(...)
//  - server knight:at(...)
//  - server knight:hp(...)
//  - server knight:tick(...)
//  - client knight:use(...)
//  - client knight:move(...)

#pragma once
#include "libpan_cxx_macros.hpp"

PAN_GH_DEFS_BEGIN

PAN_GH_MSG(PAN_GH_SERVER, knight, ability, "knight", "ability",
    PAN_GH_SLICE     id;
)
PAN_GH_DECODE(PAN_GH_SERVER, knight, ability, "knight", "ability",
    uint16_t id_len = 0;
    PAN_GH_READ(&id_len, 2);
    PAN_GH_READ_SLICE(&self->id, id_len);
)
PAN_GH_ENCODE(PAN_GH_SERVER, knight, ability, "knight", "ability",
    uint16_t len = 0;
    len += 2 + PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_HEADER("knight", "ability", len);
    uint16_t id_len = PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_WRITE(&id_len, 2);
    PAN_GH_WRITE_SLICE(&self->id, id_len);
)

PAN_GH_MSG(PAN_GH_SERVER, knight, item, "knight", "item",
    PAN_GH_SLICE     id;
)
PAN_GH_DECODE(PAN_GH_SERVER, knight, item, "knight", "item",
    uint16_t id_len = 0;
    PAN_GH_READ(&id_len, 2);
    PAN_GH_READ_SLICE(&self->id, id_len);
)
PAN_GH_ENCODE(PAN_GH_SERVER, knight, item, "knight", "item",
    uint16_t len = 0;
    len += 2 + PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_HEADER("knight", "item", len);
    uint16_t id_len = PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_WRITE(&id_len, 2);
    PAN_GH_WRITE_SLICE(&self->id, id_len);
)

PAN_GH_MSG(PAN_GH_SERVER, knight, wall, "knight", "wall",
    int32_t          x;
    int32_t          y;
)
PAN_GH_DECODE(PAN_GH_SERVER, knight, wall, "knight", "wall",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
)
PAN_GH_ENCODE(PAN_GH_SERVER, knight, wall, "knight", "wall",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    PAN_GH_HEADER("knight", "wall", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
)

PAN_GH_MSG(PAN_GH_SERVER, knight, enemy, "knight", "enemy",
    int32_t          x;
    int32_t          y;
    PAN_GH_ID        who;
    PAN_GH_CHAR64    kind;
)
PAN_GH_DECODE(PAN_GH_SERVER, knight, enemy, "knight", "enemy",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
    PAN_GH_READ(&self->who, sizeof(self->who));
    PAN_GH_READ(&self->kind, sizeof(self->kind));
)
PAN_GH_ENCODE(PAN_GH_SERVER, knight, enemy, "knight", "enemy",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    len += sizeof(self->who);
    len += sizeof(self->kind);
    PAN_GH_HEADER("knight", "enemy", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
    PAN_GH_WRITE(&self->who, sizeof(self->who));
    PAN_GH_WRITE(&self->kind, sizeof(self->kind));
)

PAN_GH_MSG(PAN_GH_SERVER, knight, root, "knight", "root",
    int32_t          x;
    int32_t          y;
    PAN_GH_ID        who;
)
PAN_GH_DECODE(PAN_GH_SERVER, knight, root, "knight", "root",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
    PAN_GH_READ(&self->who, sizeof(self->who));
)
PAN_GH_ENCODE(PAN_GH_SERVER, knight, root, "knight", "root",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    len += sizeof(self->who);
    PAN_GH_HEADER("knight", "root", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
    PAN_GH_WRITE(&self->who, sizeof(self->who));
)

PAN_GH_MSG(PAN_GH_SERVER, knight, at, "knight", "at",
    int32_t          x;
    int32_t          y;
)
PAN_GH_DECODE(PAN_GH_SERVER, knight, at, "knight", "at",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
)
PAN_GH_ENCODE(PAN_GH_SERVER, knight, at, "knight", "at",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    PAN_GH_HEADER("knight", "at", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
)

PAN_GH_MSG(PAN_GH_SERVER, knight, hp, "knight", "hp",
    int32_t          val;
)
PAN_GH_DECODE(PAN_GH_SERVER, knight, hp, "knight", "hp",
    PAN_GH_READ(&self->val, sizeof(self->val));
)
PAN_GH_ENCODE(PAN_GH_SERVER, knight, hp, "knight", "hp",
    uint16_t len = 0;
    len += sizeof(self->val);
    PAN_GH_HEADER("knight", "hp", len);
    PAN_GH_WRITE(&self->val, sizeof(self->val));
)

PAN_GH_MSG(PAN_GH_SERVER, knight, tick, "knight", "tick",
)
PAN_GH_DECODE(PAN_GH_SERVER, knight, tick, "knight", "tick",
)
PAN_GH_ENCODE(PAN_GH_SERVER, knight, tick, "knight", "tick",
    uint16_t len = 0;
    PAN_GH_HEADER("knight", "tick", len);
)

PAN_GH_MSG(PAN_GH_CLIENT, knight, use, "knight", "use",
    PAN_GH_SLICE     ability;
    PAN_GH_ID        target;
)
PAN_GH_DECODE(PAN_GH_CLIENT, knight, use, "knight", "use",
    uint16_t ability_len = 0;
    PAN_GH_READ(&ability_len, 2);
    PAN_GH_READ_SLICE(&self->ability, ability_len);
    PAN_GH_READ(&self->target, sizeof(self->target));
)
PAN_GH_ENCODE(PAN_GH_CLIENT, knight, use, "knight", "use",
    uint16_t len = 0;
    len += 2 + PAN_GH_SLICE_LEN(&self->ability);
    len += sizeof(self->target);
    PAN_GH_HEADER("knight", "use", len);
    uint16_t ability_len = PAN_GH_SLICE_LEN(&self->ability);
    PAN_GH_WRITE(&ability_len, 2);
    PAN_GH_WRITE_SLICE(&self->ability, ability_len);
    PAN_GH_WRITE(&self->target, sizeof(self->target));
)

PAN_GH_MSG(PAN_GH_CLIENT, knight, move, "knight", "move",
    int8_t           dx;
    int8_t           dy;
)
PAN_GH_DECODE(PAN_GH_CLIENT, knight, move, "knight", "move",
    PAN_GH_READ(&self->dx, sizeof(self->dx));
    PAN_GH_READ(&self->dy, sizeof(self->dy));
)
PAN_GH_ENCODE(PAN_GH_CLIENT, knight, move, "knight", "move",
    uint16_t len = 0;
    len += sizeof(self->dx);
    len += sizeof(self->dy);
    PAN_GH_HEADER("knight", "move", len);
    PAN_GH_WRITE(&self->dx, sizeof(self->dx));
    PAN_GH_WRITE(&self->dy, sizeof(self->dy));
)

PAN_GH_DEFS_END
