// GENERATED FILE -- DO NOT EDIT
// Generated by libpan
// Contains:
//  - server knight:wall(...)
//  - server knight:root(...)
//  - server knight:at(...)
//  - server knight:hp(...)
//  - server knight:tick(...)
//  - client knight:attack(...)
//  - client knight:move(...)

#pragma once
#include "libpan_cxx_macros.hpp"

PAN_GH_DEFS_BEGIN

PAN_GH_MSG(PAN_GH_SERVER, knight, wall, "knight", "wall",
    int32_t          x;
    int32_t          y;
)
PAN_GH_DECODE(PAN_GH_SERVER, knight, wall, "knight", "wall",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
)
PAN_GH_ENCODE(PAN_GH_SERVER, knight, wall, "knight", "wall",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    PAN_GH_HEADER("knight", "wall", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
)

PAN_GH_MSG(PAN_GH_SERVER, knight, root, "knight", "root",
    int32_t          x;
    int32_t          y;
    PAN_GH_ID        who;
)
PAN_GH_DECODE(PAN_GH_SERVER, knight, root, "knight", "root",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
    PAN_GH_READ(&self->who, sizeof(self->who));
)
PAN_GH_ENCODE(PAN_GH_SERVER, knight, root, "knight", "root",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    len += sizeof(self->who);
    PAN_GH_HEADER("knight", "root", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
    PAN_GH_WRITE(&self->who, sizeof(self->who));
)

PAN_GH_MSG(PAN_GH_SERVER, knight, at, "knight", "at",
    int32_t          x;
    int32_t          y;
)
PAN_GH_DECODE(PAN_GH_SERVER, knight, at, "knight", "at",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
)
PAN_GH_ENCODE(PAN_GH_SERVER, knight, at, "knight", "at",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    PAN_GH_HEADER("knight", "at", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
)

PAN_GH_MSG(PAN_GH_SERVER, knight, hp, "knight", "hp",
    int32_t          val;
)
PAN_GH_DECODE(PAN_GH_SERVER, knight, hp, "knight", "hp",
    PAN_GH_READ(&self->val, sizeof(self->val));
)
PAN_GH_ENCODE(PAN_GH_SERVER, knight, hp, "knight", "hp",
    uint16_t len = 0;
    len += sizeof(self->val);
    PAN_GH_HEADER("knight", "hp", len);
    PAN_GH_WRITE(&self->val, sizeof(self->val));
)

PAN_GH_MSG(PAN_GH_SERVER, knight, tick, "knight", "tick",
)
PAN_GH_DECODE(PAN_GH_SERVER, knight, tick, "knight", "tick",
)
PAN_GH_ENCODE(PAN_GH_SERVER, knight, tick, "knight", "tick",
    uint16_t len = 0;
    PAN_GH_HEADER("knight", "tick", len);
)

PAN_GH_MSG(PAN_GH_CLIENT, knight, attack, "knight", "attack",
    PAN_GH_ID        whom;
)
PAN_GH_DECODE(PAN_GH_CLIENT, knight, attack, "knight", "attack",
    PAN_GH_READ(&self->whom, sizeof(self->whom));
)
PAN_GH_ENCODE(PAN_GH_CLIENT, knight, attack, "knight", "attack",
    uint16_t len = 0;
    len += sizeof(self->whom);
    PAN_GH_HEADER("knight", "attack", len);
    PAN_GH_WRITE(&self->whom, sizeof(self->whom));
)

PAN_GH_MSG(PAN_GH_CLIENT, knight, move, "knight", "move",
    int8_t           dx;
    int8_t           dy;
)
PAN_GH_DECODE(PAN_GH_CLIENT, knight, move, "knight", "move",
    PAN_GH_READ(&self->dx, sizeof(self->dx));
    PAN_GH_READ(&self->dy, sizeof(self->dy));
)
PAN_GH_ENCODE(PAN_GH_CLIENT, knight, move, "knight", "move",
    uint16_t len = 0;
    len += sizeof(self->dx);
    len += sizeof(self->dy);
    PAN_GH_HEADER("knight", "move", len);
    PAN_GH_WRITE(&self->dx, sizeof(self->dx));
    PAN_GH_WRITE(&self->dy, sizeof(self->dy));
)

PAN_GH_DEFS_END
