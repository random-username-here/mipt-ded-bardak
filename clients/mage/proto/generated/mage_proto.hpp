// GENERATED FILE -- DO NOT EDIT
// Generated by libpan
// Contains:
//  - server mage:ability(...)
//  - server mage:item(...)
//  - server mage:wall(...)
//  - server mage:enemy(...)
//  - server mage:root(...)
//  - server mage:at(...)
//  - server mage:hp(...)
//  - server mage:tick(...)
//  - client mage:use(...)
//  - client mage:move(...)

#pragma once
#include "libpan_cxx_macros.hpp"

PAN_GH_DEFS_BEGIN

PAN_GH_MSG(PAN_GH_SERVER, mage, ability, "mage", "ability",
    PAN_GH_SLICE     id;
)
PAN_GH_DECODE(PAN_GH_SERVER, mage, ability, "mage", "ability",
    uint16_t id_len = 0;
    PAN_GH_READ(&id_len, 2);
    PAN_GH_READ_SLICE(&self->id, id_len);
)
PAN_GH_ENCODE(PAN_GH_SERVER, mage, ability, "mage", "ability",
    uint16_t len = 0;
    len += 2 + PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_HEADER("mage", "ability", len);
    uint16_t id_len = PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_WRITE(&id_len, 2);
    PAN_GH_WRITE_SLICE(&self->id, id_len);
)

PAN_GH_MSG(PAN_GH_SERVER, mage, item, "mage", "item",
    PAN_GH_SLICE     id;
)
PAN_GH_DECODE(PAN_GH_SERVER, mage, item, "mage", "item",
    uint16_t id_len = 0;
    PAN_GH_READ(&id_len, 2);
    PAN_GH_READ_SLICE(&self->id, id_len);
)
PAN_GH_ENCODE(PAN_GH_SERVER, mage, item, "mage", "item",
    uint16_t len = 0;
    len += 2 + PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_HEADER("mage", "item", len);
    uint16_t id_len = PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_WRITE(&id_len, 2);
    PAN_GH_WRITE_SLICE(&self->id, id_len);
)

PAN_GH_MSG(PAN_GH_SERVER, mage, wall, "mage", "wall",
    int32_t          x;
    int32_t          y;
)
PAN_GH_DECODE(PAN_GH_SERVER, mage, wall, "mage", "wall",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
)
PAN_GH_ENCODE(PAN_GH_SERVER, mage, wall, "mage", "wall",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    PAN_GH_HEADER("mage", "wall", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
)

PAN_GH_MSG(PAN_GH_SERVER, mage, enemy, "mage", "enemy",
    int32_t          x;
    int32_t          y;
    PAN_GH_ID        who;
    PAN_GH_CHAR64    kind;
)
PAN_GH_DECODE(PAN_GH_SERVER, mage, enemy, "mage", "enemy",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
    PAN_GH_READ(&self->who, sizeof(self->who));
    PAN_GH_READ(&self->kind, sizeof(self->kind));
)
PAN_GH_ENCODE(PAN_GH_SERVER, mage, enemy, "mage", "enemy",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    len += sizeof(self->who);
    len += sizeof(self->kind);
    PAN_GH_HEADER("mage", "enemy", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
    PAN_GH_WRITE(&self->who, sizeof(self->who));
    PAN_GH_WRITE(&self->kind, sizeof(self->kind));
)

PAN_GH_MSG(PAN_GH_SERVER, mage, root, "mage", "root",
    int32_t          x;
    int32_t          y;
    PAN_GH_ID        who;
)
PAN_GH_DECODE(PAN_GH_SERVER, mage, root, "mage", "root",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
    PAN_GH_READ(&self->who, sizeof(self->who));
)
PAN_GH_ENCODE(PAN_GH_SERVER, mage, root, "mage", "root",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    len += sizeof(self->who);
    PAN_GH_HEADER("mage", "root", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
    PAN_GH_WRITE(&self->who, sizeof(self->who));
)

PAN_GH_MSG(PAN_GH_SERVER, mage, at, "mage", "at",
    int32_t          x;
    int32_t          y;
)
PAN_GH_DECODE(PAN_GH_SERVER, mage, at, "mage", "at",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
)
PAN_GH_ENCODE(PAN_GH_SERVER, mage, at, "mage", "at",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    PAN_GH_HEADER("mage", "at", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
)

PAN_GH_MSG(PAN_GH_SERVER, mage, hp, "mage", "hp",
    int32_t          val;
)
PAN_GH_DECODE(PAN_GH_SERVER, mage, hp, "mage", "hp",
    PAN_GH_READ(&self->val, sizeof(self->val));
)
PAN_GH_ENCODE(PAN_GH_SERVER, mage, hp, "mage", "hp",
    uint16_t len = 0;
    len += sizeof(self->val);
    PAN_GH_HEADER("mage", "hp", len);
    PAN_GH_WRITE(&self->val, sizeof(self->val));
)

PAN_GH_MSG(PAN_GH_SERVER, mage, tick, "mage", "tick",
)
PAN_GH_DECODE(PAN_GH_SERVER, mage, tick, "mage", "tick",
)
PAN_GH_ENCODE(PAN_GH_SERVER, mage, tick, "mage", "tick",
    uint16_t len = 0;
    PAN_GH_HEADER("mage", "tick", len);
)

PAN_GH_MSG(PAN_GH_CLIENT, mage, use, "mage", "use",
    PAN_GH_SLICE     ability;
    PAN_GH_ID        target;
    int32_t          x;
    int32_t          y;
)
PAN_GH_DECODE(PAN_GH_CLIENT, mage, use, "mage", "use",
    uint16_t ability_len = 0;
    PAN_GH_READ(&ability_len, 2);
    PAN_GH_READ_SLICE(&self->ability, ability_len);
    PAN_GH_READ(&self->target, sizeof(self->target));
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
)
PAN_GH_ENCODE(PAN_GH_CLIENT, mage, use, "mage", "use",
    uint16_t len = 0;
    len += 2 + PAN_GH_SLICE_LEN(&self->ability);
    len += sizeof(self->target);
    len += sizeof(self->x);
    len += sizeof(self->y);
    PAN_GH_HEADER("mage", "use", len);
    uint16_t ability_len = PAN_GH_SLICE_LEN(&self->ability);
    PAN_GH_WRITE(&ability_len, 2);
    PAN_GH_WRITE_SLICE(&self->ability, ability_len);
    PAN_GH_WRITE(&self->target, sizeof(self->target));
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
)

PAN_GH_MSG(PAN_GH_CLIENT, mage, move, "mage", "move",
    int8_t           dx;
    int8_t           dy;
)
PAN_GH_DECODE(PAN_GH_CLIENT, mage, move, "mage", "move",
    PAN_GH_READ(&self->dx, sizeof(self->dx));
    PAN_GH_READ(&self->dy, sizeof(self->dy));
)
PAN_GH_ENCODE(PAN_GH_CLIENT, mage, move, "mage", "move",
    uint16_t len = 0;
    len += sizeof(self->dx);
    len += sizeof(self->dy);
    PAN_GH_HEADER("mage", "move", len);
    PAN_GH_WRITE(&self->dx, sizeof(self->dx));
    PAN_GH_WRITE(&self->dy, sizeof(self->dy));
)

PAN_GH_DEFS_END
