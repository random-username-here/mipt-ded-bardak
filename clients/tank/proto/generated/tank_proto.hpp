// GENERATED FILE -- DO NOT EDIT
// Generated by libpan
// Contains:
//  - server tank:wall(...)
//  - server tank:enemy(...)
//  - server tank:root(...)
//  - server tank:at(...)
//  - server tank:hp(...)
//  - server tank:tick(...)
//  - client tank:shoot(...)
//  - client tank:rotate(...)
//  - client tank:move(...)

#pragma once
#include "libpan_cxx_macros.hpp"

PAN_GH_DEFS_BEGIN

PAN_GH_MSG(PAN_GH_SERVER, tank, wall, "tank", "wall",
    int32_t          x;
    int32_t          y;
)
PAN_GH_DECODE(PAN_GH_SERVER, tank, wall, "tank", "wall",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
)
PAN_GH_ENCODE(PAN_GH_SERVER, tank, wall, "tank", "wall",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    PAN_GH_HEADER("tank", "wall", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
)

PAN_GH_MSG(PAN_GH_SERVER, tank, enemy, "tank", "enemy",
    int32_t          x;
    int32_t          y;
    PAN_GH_ID        who;
    PAN_GH_CHAR64    kind;
)
PAN_GH_DECODE(PAN_GH_SERVER, tank, enemy, "tank", "enemy",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
    PAN_GH_READ(&self->who, sizeof(self->who));
    PAN_GH_READ(&self->kind, sizeof(self->kind));
)
PAN_GH_ENCODE(PAN_GH_SERVER, tank, enemy, "tank", "enemy",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    len += sizeof(self->who);
    len += sizeof(self->kind);
    PAN_GH_HEADER("tank", "enemy", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
    PAN_GH_WRITE(&self->who, sizeof(self->who));
    PAN_GH_WRITE(&self->kind, sizeof(self->kind));
)

PAN_GH_MSG(PAN_GH_SERVER, tank, root, "tank", "root",
    int32_t          x;
    int32_t          y;
    PAN_GH_ID        who;
)
PAN_GH_DECODE(PAN_GH_SERVER, tank, root, "tank", "root",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
    PAN_GH_READ(&self->who, sizeof(self->who));
)
PAN_GH_ENCODE(PAN_GH_SERVER, tank, root, "tank", "root",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    len += sizeof(self->who);
    PAN_GH_HEADER("tank", "root", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
    PAN_GH_WRITE(&self->who, sizeof(self->who));
)

PAN_GH_MSG(PAN_GH_SERVER, tank, at, "tank", "at",
    int32_t          x;
    int32_t          y;
)
PAN_GH_DECODE(PAN_GH_SERVER, tank, at, "tank", "at",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
)
PAN_GH_ENCODE(PAN_GH_SERVER, tank, at, "tank", "at",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    PAN_GH_HEADER("tank", "at", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
)

PAN_GH_MSG(PAN_GH_SERVER, tank, hp, "tank", "hp",
    int32_t          val;
)
PAN_GH_DECODE(PAN_GH_SERVER, tank, hp, "tank", "hp",
    PAN_GH_READ(&self->val, sizeof(self->val));
)
PAN_GH_ENCODE(PAN_GH_SERVER, tank, hp, "tank", "hp",
    uint16_t len = 0;
    len += sizeof(self->val);
    PAN_GH_HEADER("tank", "hp", len);
    PAN_GH_WRITE(&self->val, sizeof(self->val));
)

PAN_GH_MSG(PAN_GH_SERVER, tank, tick, "tank", "tick",
)
PAN_GH_DECODE(PAN_GH_SERVER, tank, tick, "tank", "tick",
)
PAN_GH_ENCODE(PAN_GH_SERVER, tank, tick, "tank", "tick",
    uint16_t len = 0;
    PAN_GH_HEADER("tank", "tick", len);
)

PAN_GH_MSG(PAN_GH_CLIENT, tank, shoot, "tank", "shoot",
)
PAN_GH_DECODE(PAN_GH_CLIENT, tank, shoot, "tank", "shoot",
)
PAN_GH_ENCODE(PAN_GH_CLIENT, tank, shoot, "tank", "shoot",
    uint16_t len = 0;
    PAN_GH_HEADER("tank", "shoot", len);
)

PAN_GH_MSG(PAN_GH_CLIENT, tank, rotate, "tank", "rotate",
    int8_t           dir;
)
PAN_GH_DECODE(PAN_GH_CLIENT, tank, rotate, "tank", "rotate",
    PAN_GH_READ(&self->dir, sizeof(self->dir));
)
PAN_GH_ENCODE(PAN_GH_CLIENT, tank, rotate, "tank", "rotate",
    uint16_t len = 0;
    len += sizeof(self->dir);
    PAN_GH_HEADER("tank", "rotate", len);
    PAN_GH_WRITE(&self->dir, sizeof(self->dir));
)

PAN_GH_MSG(PAN_GH_CLIENT, tank, move, "tank", "move",
)
PAN_GH_DECODE(PAN_GH_CLIENT, tank, move, "tank", "move",
)
PAN_GH_ENCODE(PAN_GH_CLIENT, tank, move, "tank", "move",
    uint16_t len = 0;
    PAN_GH_HEADER("tank", "move", len);
)

PAN_GH_DEFS_END
