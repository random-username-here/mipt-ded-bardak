// GENERATED FILE -- DO NOT EDIT
// Generated by libpan
// Contains:
//  - server hunter:ability(...)
//  - server hunter:item(...)
//  - server hunter:wall(...)
//  - server hunter:enemy(...)
//  - server hunter:root(...)
//  - server hunter:at(...)
//  - server hunter:hp(...)
//  - server hunter:tick(...)
//  - client hunter:use(...)
//  - client hunter:move(...)

#pragma once
#include "libpan_cxx_macros.hpp"

PAN_GH_DEFS_BEGIN

PAN_GH_MSG(PAN_GH_SERVER, hunter, ability, "hunter", "ability",
    PAN_GH_SLICE     id;
)
PAN_GH_DECODE(PAN_GH_SERVER, hunter, ability, "hunter", "ability",
    uint16_t id_len = 0;
    PAN_GH_READ(&id_len, 2);
    PAN_GH_READ_SLICE(&self->id, id_len);
)
PAN_GH_ENCODE(PAN_GH_SERVER, hunter, ability, "hunter", "ability",
    uint16_t len = 0;
    len += 2 + PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_HEADER("hunter", "ability", len);
    uint16_t id_len = PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_WRITE(&id_len, 2);
    PAN_GH_WRITE_SLICE(&self->id, id_len);
)

PAN_GH_MSG(PAN_GH_SERVER, hunter, item, "hunter", "item",
    PAN_GH_SLICE     id;
)
PAN_GH_DECODE(PAN_GH_SERVER, hunter, item, "hunter", "item",
    uint16_t id_len = 0;
    PAN_GH_READ(&id_len, 2);
    PAN_GH_READ_SLICE(&self->id, id_len);
)
PAN_GH_ENCODE(PAN_GH_SERVER, hunter, item, "hunter", "item",
    uint16_t len = 0;
    len += 2 + PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_HEADER("hunter", "item", len);
    uint16_t id_len = PAN_GH_SLICE_LEN(&self->id);
    PAN_GH_WRITE(&id_len, 2);
    PAN_GH_WRITE_SLICE(&self->id, id_len);
)

PAN_GH_MSG(PAN_GH_SERVER, hunter, wall, "hunter", "wall",
    int32_t          x;
    int32_t          y;
)
PAN_GH_DECODE(PAN_GH_SERVER, hunter, wall, "hunter", "wall",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
)
PAN_GH_ENCODE(PAN_GH_SERVER, hunter, wall, "hunter", "wall",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    PAN_GH_HEADER("hunter", "wall", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
)

PAN_GH_MSG(PAN_GH_SERVER, hunter, enemy, "hunter", "enemy",
    int32_t          x;
    int32_t          y;
    PAN_GH_ID        who;
    PAN_GH_CHAR64    kind;
)
PAN_GH_DECODE(PAN_GH_SERVER, hunter, enemy, "hunter", "enemy",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
    PAN_GH_READ(&self->who, sizeof(self->who));
    PAN_GH_READ(&self->kind, sizeof(self->kind));
)
PAN_GH_ENCODE(PAN_GH_SERVER, hunter, enemy, "hunter", "enemy",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    len += sizeof(self->who);
    len += sizeof(self->kind);
    PAN_GH_HEADER("hunter", "enemy", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
    PAN_GH_WRITE(&self->who, sizeof(self->who));
    PAN_GH_WRITE(&self->kind, sizeof(self->kind));
)

PAN_GH_MSG(PAN_GH_SERVER, hunter, root, "hunter", "root",
    int32_t          x;
    int32_t          y;
    PAN_GH_ID        who;
)
PAN_GH_DECODE(PAN_GH_SERVER, hunter, root, "hunter", "root",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
    PAN_GH_READ(&self->who, sizeof(self->who));
)
PAN_GH_ENCODE(PAN_GH_SERVER, hunter, root, "hunter", "root",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    len += sizeof(self->who);
    PAN_GH_HEADER("hunter", "root", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
    PAN_GH_WRITE(&self->who, sizeof(self->who));
)

PAN_GH_MSG(PAN_GH_SERVER, hunter, at, "hunter", "at",
    int32_t          x;
    int32_t          y;
)
PAN_GH_DECODE(PAN_GH_SERVER, hunter, at, "hunter", "at",
    PAN_GH_READ(&self->x, sizeof(self->x));
    PAN_GH_READ(&self->y, sizeof(self->y));
)
PAN_GH_ENCODE(PAN_GH_SERVER, hunter, at, "hunter", "at",
    uint16_t len = 0;
    len += sizeof(self->x);
    len += sizeof(self->y);
    PAN_GH_HEADER("hunter", "at", len);
    PAN_GH_WRITE(&self->x, sizeof(self->x));
    PAN_GH_WRITE(&self->y, sizeof(self->y));
)

PAN_GH_MSG(PAN_GH_SERVER, hunter, hp, "hunter", "hp",
    int32_t          val;
)
PAN_GH_DECODE(PAN_GH_SERVER, hunter, hp, "hunter", "hp",
    PAN_GH_READ(&self->val, sizeof(self->val));
)
PAN_GH_ENCODE(PAN_GH_SERVER, hunter, hp, "hunter", "hp",
    uint16_t len = 0;
    len += sizeof(self->val);
    PAN_GH_HEADER("hunter", "hp", len);
    PAN_GH_WRITE(&self->val, sizeof(self->val));
)

PAN_GH_MSG(PAN_GH_SERVER, hunter, tick, "hunter", "tick",
)
PAN_GH_DECODE(PAN_GH_SERVER, hunter, tick, "hunter", "tick",
)
PAN_GH_ENCODE(PAN_GH_SERVER, hunter, tick, "hunter", "tick",
    uint16_t len = 0;
    PAN_GH_HEADER("hunter", "tick", len);
)

PAN_GH_MSG(PAN_GH_CLIENT, hunter, use, "hunter", "use",
    PAN_GH_SLICE     ability;
    PAN_GH_ID        target;
)
PAN_GH_DECODE(PAN_GH_CLIENT, hunter, use, "hunter", "use",
    uint16_t ability_len = 0;
    PAN_GH_READ(&ability_len, 2);
    PAN_GH_READ_SLICE(&self->ability, ability_len);
    PAN_GH_READ(&self->target, sizeof(self->target));
)
PAN_GH_ENCODE(PAN_GH_CLIENT, hunter, use, "hunter", "use",
    uint16_t len = 0;
    len += 2 + PAN_GH_SLICE_LEN(&self->ability);
    len += sizeof(self->target);
    PAN_GH_HEADER("hunter", "use", len);
    uint16_t ability_len = PAN_GH_SLICE_LEN(&self->ability);
    PAN_GH_WRITE(&ability_len, 2);
    PAN_GH_WRITE_SLICE(&self->ability, ability_len);
    PAN_GH_WRITE(&self->target, sizeof(self->target));
)

PAN_GH_MSG(PAN_GH_CLIENT, hunter, move, "hunter", "move",
    int8_t           dx;
    int8_t           dy;
)
PAN_GH_DECODE(PAN_GH_CLIENT, hunter, move, "hunter", "move",
    PAN_GH_READ(&self->dx, sizeof(self->dx));
    PAN_GH_READ(&self->dy, sizeof(self->dy));
)
PAN_GH_ENCODE(PAN_GH_CLIENT, hunter, move, "hunter", "move",
    uint16_t len = 0;
    len += sizeof(self->dx);
    len += sizeof(self->dy);
    PAN_GH_HEADER("hunter", "move", len);
    PAN_GH_WRITE(&self->dx, sizeof(self->dx));
    PAN_GH_WRITE(&self->dy, sizeof(self->dy));
)

PAN_GH_DEFS_END
