name: build action
on: [push, pull_request]
jobs:
  build-cmake:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        compiler: [g++, clang++]
        build_type: [Debug, Release]
    steps:
      - name: install deps
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake libinih-dev \
            libasound2-dev libx11-dev libxrandr-dev libxi-dev \
            libgl1-mesa-dev libxcursor-dev libxinerama-dev
      - name: raylib
        run: |
          git clone --depth 1 https://github.com/raysan5/raylib.git
          cd raylib
          mkdir build && cd build
          cmake ..
          make
          sudo make install RAYLIB_LIBTYPE=SHARED
          sudo ldconfig
      - name: clone
        id: checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: build
        run: |
          cmake -S . -B build -D CMAKE_BUILD_TYPE=${{matrix.build_type}} -D CMAKE_CXX_COMPILER=${{matrix.compiler}} -D BUILD_GRAPHICS=OFF
          cmake --build build
  tests:
    needs: build-cmake
    runs-on: ubuntu-latest
    strategy:
      matrix:
        compiler: [g++, clang++]
        build_type: [Debug, Release]
    if: ${{ success() }}
    steps:
      - name: install deps
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential cmake libinih-dev \
            libasound2-dev libx11-dev libxrandr-dev libxi-dev \
            libgl1-mesa-dev libxcursor-dev libxinerama-dev
      - name: raylib
        run: |
          git clone --depth 1 https://github.com/raysan5/raylib.git
          cd raylib
          mkdir build && cd build
          cmake ..
          make
          sudo make install RAYLIB_LIBTYPE=SHARED
          sudo ldconfig
      - name: clone
        id: checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: build
        run: |
          cmake -S . -B build -D CMAKE_BUILD_TYPE=${{matrix.build_type}} -D CMAKE_CXX_COMPILER=${{matrix.compiler}} -D BUILD_GRAPHICS=OFF
          cmake --build build
      - name: move mod dirs
        run: |
          mkdir ./CItest
          cp -r ./build/* ./CItest
          rm -r -f ./CItest/mods/tty-graphics
      - name: test
        run: |
          SERVER_IP=$(hostname -I | awk '{print $1}')
          ./build/servers/msva/msva ./.github/CItest.ini &
          SERVER_PID=$!

          sleep 2

          ./build/clients/random/random_client $SERVER_IP 3000 &
          CLIENT_PID=$!

          sleep 2

          kill $SERVER_PID 2>/dev/null || true
          wait $SERVER_PID 2>/dev/null || true

          wait $CLIENT_PID 2>/dev/null
          CLIENT_RESULT=$?

          exit $CLIENT_RESULT

  footjob:
    needs: [build-cmake, tests]
    runs-on: ubuntu-latest
    if: ${{ success() }}
    steps:
      - name: chill-drill
        run: |
          echo Hello foot world
